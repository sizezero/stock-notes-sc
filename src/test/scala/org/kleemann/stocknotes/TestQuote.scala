package org.kleemann.stocknotes

class TestQuote extends munit.FunSuite {
    test("need to do something...") {
        // most of the functions have side effects so they're not really testable
        assert(true)
    }
}
